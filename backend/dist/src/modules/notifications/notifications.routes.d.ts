import { Router } from 'express';
import { NotificationController } from './notifications.controller';
/**
 * Creates and configures a router for notification endpoints
 * @param controller - The notification controller instance
 * @returns The configured Express router
 */
export declare const createNotificationRouter: (controller: NotificationController) => Router;
//# sourceMappingURL=notifications.routes.d.ts.map