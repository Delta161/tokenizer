import { Router } from 'express';
/**
 * Create and configure the flags router
 */
export declare const createFlagsRouter: () => Router;
export declare const flagsRouter: Router;
/**
 * Mount flags routes to an existing Express app
 * @param app Express application
 * @param basePath Base path for mounting the router
 */
export declare const mountFlagsRoutes: (app: any, basePath?: string) => void;
//# sourceMappingURL=flags.routes.d.ts.map